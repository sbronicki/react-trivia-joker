import React from 'react'
import classes from './Card.module.css'

const Card = (props) => (
    <div className={classes.Card}>
        <h1>Card</h1>
        <h3>Question: etc etc</h3>
    </div>
)

export default Card
